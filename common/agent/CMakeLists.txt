set(TARGET llama-agent-poc)
add_library(${TARGET} agent-bootstrap.cpp agent-bootstrap.h agent-package-json.cpp agent-package-json.h agent-prepared-generation.cpp agent-prepared-generation.h blueprint-selector.cpp blueprint-selector.h deliberation-policy.cpp deliberation-policy.h agent-runtime-context.h agent-runtime-reflection-phase.cpp agent-runtime-research-phase.cpp agent-runtime.cpp agent-runtime.h runtime-json-contracts.cpp runtime-json-contracts.h memory-learning.cpp memory-learning.h reflection-json.cpp reflection-json.h schema-contract.cpp schema-contract.h sandbox-docker-runtime.cpp sandbox-docker-runtime.h sandbox-kubernetes-runtime.cpp sandbox-kubernetes-runtime.h tool-adapters.cpp tool-adapters.h tool-catalog.cpp tool-catalog.h tool-chat-bridge.cpp tool-chat-bridge.h tool-registry.cpp tool-registry.h tool-result-contracts.cpp tool-result-contracts.h research/research-contract.h research/research-workspace.cpp research/research-workspace.h research/research-workspace-factory.cpp research/research-workspace-factory.h research/research-controller.cpp research/research-controller.h research/research-runner.cpp research/research-runner.h)
target_include_directories(${TARGET} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_compile_features(${TARGET} PUBLIC cxx_std_17)
target_link_libraries(${TARGET} PUBLIC llama-plan llama-memory)
target_link_libraries(${TARGET} PRIVATE cpp-httplib)
if (WIN32)
    target_compile_definitions(${TARGET} PRIVATE LLAMA_AGENT_WEB_USE_WINHTTP=1)
    target_link_libraries(${TARGET} PRIVATE winhttp)
endif()
if (BUILD_SHARED_LIBS)
    set_target_properties(${TARGET} PROPERTIES POSITION_INDEPENDENT_CODE ON)
    set_target_properties(${TARGET} PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
endif()
