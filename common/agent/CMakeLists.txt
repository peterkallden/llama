set(TARGET llama-agent-poc)
add_library(${TARGET} agent-bootstrap.cpp agent-bootstrap.h agent-package-json.cpp agent-package-json.h blueprint-selector.cpp blueprint-selector.h agent-runtime.cpp agent-runtime.h memory-learning.cpp memory-learning.h reflection-json.cpp reflection-json.h tool-adapters.cpp tool-adapters.h tool-catalog.cpp tool-catalog.h tool-chat-bridge.cpp tool-chat-bridge.h tool-registry.cpp tool-registry.h)
target_include_directories(${TARGET} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_compile_features(${TARGET} PUBLIC cxx_std_17)
target_link_libraries(${TARGET} PUBLIC llama-plan llama-memory)
if (BUILD_SHARED_LIBS)
    set_target_properties(${TARGET} PROPERTIES POSITION_INDEPENDENT_CODE ON)
    set_target_properties(${TARGET} PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
endif()
