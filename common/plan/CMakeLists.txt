set(TARGET llama-agent-plan)
add_library(${TARGET} plan-bindings.cpp plan-bindings.h plan-contract.cpp plan-contract.h plan-blueprint.cpp plan-blueprint.h plan-context.cpp plan-context.h plan-goal.cpp plan-goal.h plan-in-memory.cpp plan-in-memory.h plan-json.cpp plan-json.h plan-memory.cpp plan-memory.h plan-policy.cpp plan-policy.h plan-scheduler.cpp plan-scheduler.h plan-selection.cpp plan-store.h plan-template.cpp plan-template.h plan-types.h)
add_library(llama-plan ALIAS ${TARGET})
target_include_directories(${TARGET} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_compile_features(${TARGET} PUBLIC cxx_std_17)
target_link_libraries(${TARGET} PUBLIC llama-agent-memory)
if (BUILD_SHARED_LIBS)
    set_target_properties(${TARGET} PROPERTIES POSITION_INDEPENDENT_CODE ON)
    set_target_properties(${TARGET} PROPERTIES WINDOWS_EXPORT_ALL_SYMBOLS ON)
endif()
if (LLAMA_PLAN_COZO)
    if (NOT COZO_INCLUDE_DIR)
        find_path(COZO_INCLUDE_DIR cozo_c.h)
    endif()
    if (NOT COZO_LIBRARY)
        find_library(COZO_LIBRARY NAMES cozo_c cozo)
    endif()
    if (NOT COZO_INCLUDE_DIR OR NOT COZO_LIBRARY)
        message(FATAL_ERROR "LLAMA_PLAN_COZO=ON requires Cozo C API headers and library. Set COZO_INCLUDE_DIR and COZO_LIBRARY.")
    endif()
    target_sources(${TARGET} PRIVATE cozo/plan-cozo.cpp cozo/plan-cozo.h cozo/plan-cozo-schema.cpp cozo/plan-cozo-schema.h)
    target_compile_definitions(${TARGET} PUBLIC LLAMA_PLAN_USE_COZO)
    llama_link_cozo(${TARGET})
endif()

if (LLAMA_AGENT_STORAGE_SQLITE)
    target_sources(${TARGET} PRIVATE sqlite/plan-sqlite.cpp sqlite/plan-sqlite.h)
    target_compile_definitions(${TARGET} PUBLIC LLAMA_PLAN_USE_SQLITE)
    target_link_libraries(${TARGET} PUBLIC llama-agent-storage-sqlite)
endif()
