# Install a CUDA-compatible PyTorch build separately for the intended driver
# and CUDA runtime. The adaptation worker deliberately does not vendor it.
transformers>=4.46,<5
peft>=0.13,<1
accelerate>=1,<2
bitsandbytes>=0.44,<1
safetensors>=0.4,<1
