#include "plan/plan-template.h"
#include <cassert>
int main() { common_plan_template t; t.id="verify"; t.goal="Answer safely"; t.success_criteria="Verified answer"; t.next_action="retrieve"; common_plan_step retrieve; retrieve.id="retrieve"; retrieve.title="Retrieve"; retrieve.objective="Find evidence"; common_plan_step answer; answer.id="answer"; answer.title="Answer"; answer.objective="Respond"; answer.depends_on={"retrieve"}; answer.result_summary="old result"; t.steps={retrieve,answer}; t.constraints.push_back({"safe","Use evidence",true}); t.assumptions.push_back({"a","Source is available",0.5f,false,{"memory:old"}}); common_plan_state plan; std::string error; common_plan_template_options options; options.scope=common_plan_scope::global; options.now=42; assert(common_plan_instantiate_template(t,"plan-42","local-instance",plan,error,options)); assert(plan.id=="plan-42" && plan.scope==common_plan_scope::global && plan.status==common_plan_status::active); assert(plan.steps[0].id=="plan-42:retrieve" && plan.steps[0].status==common_plan_step_status::active); assert(plan.steps[1].depends_on[0]=="plan-42:retrieve" && !plan.steps[1].result_summary); assert(plan.observations.empty() && plan.version==0); assert(plan.assumptions[0].id=="plan-42:a" && plan.assumptions[0].valid); assert(!common_plan_instantiate_template(t,"","local-instance",plan,error)); return 0; }
