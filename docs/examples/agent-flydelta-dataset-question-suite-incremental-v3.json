{
  "schema_version": 1,
  "id": "flydelta://fixture/dataset-question-suite-incremental-v3",
  "suite_role": "incremental",
  "minimum_distinct_tools": 8,
  "language": "en",
  "dataset": "dataset://local/sales",
  "dataset_name": "Sales",
  "purpose": "Third incremental batch of new model-facing dataset intentions for FlyDelta search, repair and host-certified aggregation.",
  "model_contract": {
    "selection": "Choose exactly one most-specific read-only dataset tool. Return only {\"name\":\"tool.name\",\"arguments\":{...}}.",
    "dataset_rule": "Use the canonical dataset reference dataset://local/sales when a dataset argument is required.",
    "learning_rule": "A model selection is only an observation. A FlyDelta learning example requires a separate host-verified repair or counterfactual."
  },
  "scenarios": [
    {
      "id": "north-high-value-window",
      "question": "Return order_id, region, and amount for north-region orders with amount at least 8. Sort by amount from low to high and keep at most two rows.",
      "expected_tool": "data.query",
      "plan": {
        "goal": "Project, filter, sort and bound a result",
        "steps": [
          {"tool": "data.query", "args": {"dataset": "dataset://local/sales", "select": ["order_id", "region", "amount"], "where": [{"field": "region", "operator": "=", "value": "north"}, {"field": "amount", "operator": ">=", "value": 8}], "order_by": [{"field": "amount", "direction": "asc"}], "limit": 2}}
        ]
      }
    },
    {
      "id": "non-south-multi-condition-filter",
      "question": "Keep the records that are not from the south region and have at least two units. Preserve the original columns and return a bounded result.",
      "expected_tool": "data.filter",
      "plan": {
        "goal": "Apply multiple declarative filter conditions",
        "steps": [
          {"tool": "data.filter", "args": {"dataset": "dataset://local/sales", "conditions": [{"field": "region", "operator": "!=", "value": "south"}, {"field": "units", "operator": ">=", "value": 2}], "limit": 100}}
        ]
      }
    },
    {
      "id": "customer-region-order-counts",
      "question": "Count orders for each customer within each region. Return one row per customer and region.",
      "expected_tool": "data.aggregate",
      "plan": {
        "goal": "Count records across two grouping dimensions",
        "steps": [
          {"tool": "data.aggregate", "args": {"dataset": "dataset://local/sales", "group_by": ["customer_id", "region"], "measures": [{"function": "count", "column": "*", "as": "order_count"}]}}
        ]
      }
    },
    {
      "id": "regional-amount-range",
      "question": "For every region, report the number of orders, the smallest amount, and the largest amount.",
      "expected_tool": "data.aggregate",
      "plan": {
        "goal": "Compute several grouped measures in one pass",
        "steps": [
          {"tool": "data.aggregate", "args": {"dataset": "dataset://local/sales", "group_by": ["region"], "measures": [{"function": "count", "column": "*", "as": "order_count"}, {"function": "min", "column": "amount", "as": "minimum_amount"}, {"function": "max", "column": "amount", "as": "maximum_amount"}]}}
        ]
      }
    },
    {
      "id": "regional-amount-and-units-profile",
      "question": "Describe both amount and units separately within each region, including counts and numeric ranges.",
      "expected_tool": "statistics.describe",
      "plan": {
        "goal": "Describe multiple numeric columns by group",
        "steps": [
          {"tool": "statistics.describe", "args": {"dataset": "dataset://local/sales", "columns": ["amount", "units"], "group_by": ["region"]}}
        ]
      }
    },
    {
      "id": "regional-amount-outliers",
      "question": "Find amount outliers independently inside each region using the IQR method with a multiplier of two.",
      "expected_tool": "statistics.outliers",
      "plan": {
        "goal": "Detect grouped numeric outliers with an explicit threshold",
        "steps": [
          {"tool": "statistics.outliers", "args": {"dataset": "dataset://local/sales", "column": "amount", "group_by": ["region"], "method": "iqr", "multiplier": 2.0}}
        ]
      }
    },
    {
      "id": "customer-frequency-table",
      "question": "Return the frequency of each customer_id value, including the number of distinct customers.",
      "expected_tool": "statistics.value_counts",
      "plan": {
        "goal": "Build a bounded frequency table for a key column",
        "steps": [
          {"tool": "statistics.value_counts", "args": {"dataset": "dataset://local/sales", "column": "customer_id", "limit": 100}}
        ]
      }
    },
    {
      "id": "ordered-distinct-regions",
      "question": "Return each region exactly once, sorted alphabetically, and do not include any other columns.",
      "expected_tool": "data.query",
      "plan": {
        "goal": "Return an ordered distinct projection",
        "steps": [
          {"tool": "data.query", "args": {"dataset": "dataset://local/sales", "select": ["region"], "distinct": true, "order_by": [{"field": "region", "direction": "asc"}], "limit": 16}}
        ]
      }
    },
    {
      "id": "bounded-schema-contract",
      "question": "Confirm the typed column schema for Sales with a bounded host inspection. I need column definitions, not example rows.",
      "expected_tool": "dataset.schema",
      "plan": {
        "goal": "Read a bounded typed schema before selecting columns",
        "steps": [
          {"tool": "dataset.schema", "args": {"dataset": "dataset://local/sales", "max_rows": 32}}
        ]
      }
    },
    {
      "id": "report-quantity-column-shape",
      "question": "Prepare a compact report by dropping region and renaming units to quantity. Return the transformed rows.",
      "expected_tool": "data.transform",
      "plan": {
        "goal": "Apply a drop followed by a rename transformation",
        "steps": [
          {"tool": "data.transform", "args": {"dataset": "dataset://local/sales", "operations": [{"type": "drop", "column": "region"}, {"type": "rename", "from": "units", "to": "quantity"}]}}
        ]
      }
    },
    {
      "id": "lowest-unit-orders",
      "question": "Show the two orders with the fewest units, returning only order_id, customer_id, and units in ascending units order.",
      "expected_tool": "data.query",
      "plan": {
        "goal": "Find a bounded minimum by sorting a projected result",
        "steps": [
          {"tool": "data.query", "args": {"dataset": "dataset://local/sales", "select": ["order_id", "customer_id", "units"], "order_by": [{"field": "units", "direction": "asc"}], "limit": 2}}
        ]
      }
    },
    {
      "id": "east-or-north-amount-filter",
      "question": "Keep orders whose region is east or north and whose amount is greater than 3. Return the matching original records.",
      "expected_tool": "data.filter",
      "plan": {
        "goal": "Filter with a membership predicate and a numeric threshold",
        "steps": [
          {"tool": "data.filter", "args": {"dataset": "dataset://local/sales", "conditions": [{"field": "region", "operator": "in", "value": ["east", "north"]}, {"field": "amount", "operator": ">", "value": 3}], "limit": 100}}
        ]
      }
    }
  ]
}
